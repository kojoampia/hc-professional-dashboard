import { Injectable } from '@angular/core';
// import { IMetricItem } from './metric-item.model';

@Injectable({
  providedIn: 'root',
})
export class MetricPanelService {
  constructor() {}

  /* ----- Create Mock Data -----
    createMockData(): IMetricItem[]{
        const metricItems: IMetricItem[] = [];
        metricItems.push({name:'Revenue This Month',label:'metric-panel.revenue',value:11000});
        metricItems.push({name:'Pending Applications',label:'metric-panel.applications',value:84});
        metricItems.push({name:'Pending Claims',label:'metric-panel.claims',value:34});
        metricItems.push({name:'New Customers',label:'metric-panel.customers',value:62});
        return metricItems;
    }*/
}
